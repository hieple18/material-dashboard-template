export default theme => ({
  root: {
    // backgroundColor: theme.palette.background.default,
    overflow: 'hidden'
  },
  foreground: {
    position: 'absolute',
    top: 0,
    left: 0,
    minWidth: '100%',
    minHeight: '100%',
    backgroundColor: '#E05376',
  }
});
