
export default theme => ({
    wrapper: {
      position: 'relative',
    },
    speedDial: {
      position: 'absolute',
      top: theme.spacing(2),
      right: theme.spacing(3),
      // '&$directionUp, &$directionLeft': {
      // },
    },
});